function galleryEffect() {
	gsapCounter.registerPlugin(SrollTrigger);
	let effectBoxs = document.querySelectorAll('workBox');

}

// 페이지가 로드된 후 실행
document.addEventListener('DOMContentLoaded', runMainEffect);
